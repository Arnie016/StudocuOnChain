# Loki Unchained Interface

React dApp for the Loki Unchained on-chain notes marketplace.

## Scripts

```bash
npm install
npm start
npm run build
```

## Environment

Use `.env.example` as the base for local deployment settings. Contract actions are enabled only when the connected wallet is on `REACT_APP_SUPPORTED_CHAIN_ID`.
